(require 'cl)
;; set fontset-startup as current frame's font.
;;(set-frame-font "fontset-startup" nil t)
;; set fontset-startup as default frame font.
;;(add-to-list 'default-frame-alist '(font . "fontset-startup"))
;;(set-default-font "文泉驿微米黑-12")

(provide 'init-fonts)
